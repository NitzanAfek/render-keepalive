name: Keep Render Alive
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Slack ticket bot health endpoint
        run: curl -s https://slack-ticket-bot-ix6p.onrender.com/health
